# Вариант tvbox (ветка tvbox): без torch/whisper/postgres — чистый Python,
# ставится на armv7l/1 ГБ без компиляции. Транскрибация через OpenAI audio API.

# --- Пайплайн devtrade_parse ---
openai
python-dotenv
yt-dlp
requests

# --- Telegram-бот devtrade_bot ---
aiogram>=3.4
SQLAlchemy[asyncio]>=2.0
aiosqlite
greenlet

# ffmpeg ставится системно: apt install ffmpeg (не через pip)
